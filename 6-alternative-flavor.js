// we can export direcly on the same line
module.exports.items = ['item1', 'item2']

// or we can declare the variable first and then export
const person = {
  name: 'Bob',
}

module.exports.singlePerson = person